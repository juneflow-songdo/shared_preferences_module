name: shared_preferences_module
description: "this module is used to create shared_preferences module for juneflow."
#publish_to: 'none'
repository: https://github.com/juneflow-songdo/shared_preferences_module

version: 1.0.0

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  go_router:
  june_flow_util:
  shared_preferences: #@add

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - assets/module/shared_preferences_module/
  uses-material-design: true